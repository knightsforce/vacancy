import React, { Component } from 'react';
import { connect } from 'react-redux';

class VacanciesList extends Component {
	constructor(props) {
		super(props);
	}

	render() {
		return (
			<div/>
		);
	}
};

export default connect()(VacanciesList);